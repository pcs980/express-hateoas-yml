language: node_js
node_js:
  - "10"
  - "8"
jobs:
  include:
    - stage: npm release
      node_js: "10"
      deploy:
        provider: npm
        email: $NPM_EMAIL
        api_key: $NPM_API_KEY
        skip_cleanup: true
        on:
          branch: master