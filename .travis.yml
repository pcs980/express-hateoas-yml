language: node_js
node_js:
  - "10"
  - "8"
addons:
  code_climate:
    repo_token: $CC_REPORTER_ID
jobs:
  include:
    - stage: npm release
      node_js: "10"
      deploy:
        provider: npm
        email: $NPM_EMAIL
        api_key: $NPM_API_KEY
