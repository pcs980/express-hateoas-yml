language: node_js
node_js:
  - "10"
  - "8"
before_scripts:
  - npm install
after_success:
  - npm run coverage
jobs:
  include:
    - stage: npm release
      if: branch = master
      deploy:
        provider: npm
        email: $NPM_EMAIL
        api_key: $NPM_API_KEY
